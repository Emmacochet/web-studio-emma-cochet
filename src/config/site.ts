// TODO: update once a domain is registered (studioemmacochet.fr or .com),
// then point it at GitHub Pages via a CNAME file and drop the basePath
// hack in next.config.ts (a custom domain serves from "/", not "/repo-name/").
export const siteUrl = "https://studioemmacochet.fr";
