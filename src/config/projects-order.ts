// Slugs (src/projects/<slug>) in the order they should appear on the
// projects page. Any project not listed here is appended after, sorted
// alphabetically by slug.
export const projectsOrder: string[] = ["gaite", "robespierre_montreuil", "algaetecture"];
